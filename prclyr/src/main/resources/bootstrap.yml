spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: msvc-process
      reload:
        enabled: true
        mode: event
        strategy: refresh        